# 📋 Roadmap развития Unkaos (актуализировано под текущий код)

Этот документ заменяет предыдущий “wishlist” и отражает **то, что реально уже есть в репозитории**, а также **что логично делать дальше** с учётом архитектуры (микросервисы: `hecate` → `cerberus` → `zeus2` + `hermes` + `athena` + `ossa` + `aether`, фронт `www` на Vue).

*Обновлено: декабрь 2025*

---

## ✅ Что уже реализовано (важно для приоритезации)

- **API v2 (`zeus2`)**: CRUD Factory, Prisma, фильтры через `@unkaos/query-lang`, rate limiting, health endpoints.
- **Agile доски / дашборды / гаджеты**: доски, дашборды, гаджеты `Burndown`, `TimeReport`, `IssuesTable`.
- **Time tracking (база)**: сущность `time_entries` + UI просмотра/добавления/редактирования.
- **Комментарии/история изменений**: `issue_actions` (комментарии, смены статусов, правки полей), UI лента действий.
- **Уведомления**: `hermes` (email/мессенджеры), watchers.
- **AI**: `athena` + UI-панель.
- **RBAC (база)**: роли + permissions + project-permissions (есть UI и проверка через `cerberus`).

---

## 🧯 Критика прежнего плана (почему переписан)

- **Нет приоритетов/зависимостей**: рядом стоят “починить баг доски” и “SAML/real-time co-editing”.
- **Часть пунктов уже реализована** (дашборды, time reports, markdown-редактор и т.д.) — план дублировал реальность.
- **Мало фундамента**: целостность данных при удалении, безопасность auth, публичная документация API — это must-have до “космических” фич.

---

## 🎯 Приоритизированный roadmap (эти пункты создаём как задачи в трекере)

### P0 — Стабильность + безопасность + интеграционный контур (2–4 недели)

- [ ] **P0.1 Доска: стабильный порядок карточек**
  - Где: `www/src/views/PageBoard.vue`
  - Зачем: сейчас порядок хранится/пересчитывается нестабильно, есть явные edge-case'ы и баги сравнения позиций.
  - Готово когда:
    - порядок **сохраняется** после перезагрузки/смены фильтров/drag между колонками
    - нет “перескоков” при отсутствии `position`

- [ ] **P0.2 Контроль зависимостей при удалении (backend + UI)**
  - Где: `server/zeus2/utils/crud-factory.ts` (+ админские страницы `www/src/views/*`)
  - Готово когда:
    - при удалении “используемых” сущностей возвращается **понятная 409/400** с перечнем зависимостей
    - UI показывает “где используется” и предлагает безопасную альтернативу (архивация/замена)

- [ ] **P0.3 Security hardening Cerberus (JWT secret + пароли)**
  - Где: `server/cerberus/security.ts`
  - Почему срочно: сейчас MD5 и статический JWT ключ — это блокер для enterprise и риск для публичной установки.
  - Готово когда:
    - секрет JWT берётся из env
    - пароли — не MD5 (bcrypt/argon2), есть миграционный план/совместимость

- [ ] **P0.4 OpenAPI/Swagger для API v2**
  - Где: `server/zeus2`
  - Готово когда:
    - есть `/api/v2/openapi.json` и Swagger UI (или статичная публикация)
    - покрыты ключевые сущности: issues, projects, fields, workflows, boards, sprints, time_entries

- [ ] **P0.5 Исходящие Webhooks (MVP)**
  - Где: `zeus2` (события) + конфигурация в БД + отправка (возможно `hermes` как “integrations hub”)
  - Готово когда:
    - можно подписаться на события (issue created/updated, comment added, status changed)
    - есть retry/backoff и журнал доставок

- [ ] **P0.6 Тесты (регрессия на критичные P0-фичи)**
  - Где: `server/asclepius` (E2E) + `server/zeus2/__tests__`
  - Готово когда:
    - есть E2E, воспроизводящий порядок на доске
    - есть тесты/кейсы на запрет удаления при зависимостях

### P1 — Git-интеграции для dev-команд (4–8 недель)

- [ ] **P1.1 GitLab интеграция: поднять webhook-обработчик (MVP)**
  - Где: `server/hermes/GitlabCommit.ts` (сейчас закомментирован), `hermes` REST
  - Готово когда:
    - принимаем события GitLab (коммиты/MR)
    - достаём ключи задач (`BS-123`) из сообщений/веток
    - сохраняем связи (минимально: таблица “git_events”/“issue_git_links”)

- [ ] **P1.2 UI: отображать Git-активность в карточке задачи**
  - Где: `www/src/views/PageIssue.vue` (+ компоненты)
  - Готово когда:
    - видно список коммитов/MR/веток, привязанных к задаче

- [ ] **P1.3 Автопереход статусов по Git-событиям**
  - Где: `hermes`/`zeus2` (смена статуса) + настройка маппинга
  - Готово когда:
    - настраиваем “MR merged → Завершена” (или через workflow transitions)
    - все действия логируются как `issue_action`

- [ ] **P1.4 “Создать ветку” из задачи (GitLab)**
  - Готово когда:
    - кнопка в UI создаёт ветку по шаблону имени
    - поддержка project-level настроек репозитория

### P2 — Автоматизации (Hephaestus) (8–12 недель)

- [ ] **P2.1 Модель и CRUD для автоматизаций**
  - Где: `www/src/views/PageAutomations.vue` (сейчас просто CRUD-форма), `zeus2` роуты/таблицы
  - Готово когда:
    - правило хранит trigger/conditions/actions (JSON)
    - есть валидация и предпросмотр “что сработает”

- [ ] **P2.2 Запускатель правил (hephaestus)**
  - Где: `server/hephaestus/index.ts` (сейчас заглушка)
  - Готово когда:
    - поддержаны минимум триггеры: issue created / issue updated / cron
    - минимум действия: изменить поле, создать задачу, добавить комментарий, вызвать webhook

- [ ] **P2.3 Логи выполнения и история автоматизаций**
  - Готово когда:
    - видно кто/что/почему сработало, с payload и результатом

---

## 🗃️ Backlog идей (пока без нарезки в трекер)

- Gantt-гаджет для дашбордов (на базе relations + start/end/due)
- Time tracking 2.0 (таймер, округления, напоминания, экспорт)
- PWA/мобильный UX (адаптивность + пуши)
- Enterprise: audit log, field-level RBAC, 2FA, SSO (OAuth2/SAML), IP allowlist
- Импорт/экспорт: YouTrack/Jira/Trello/Asana + full-backup воркспейса + маппинг полей
- Wiki/база знаний (pages tree, search, versioning)

