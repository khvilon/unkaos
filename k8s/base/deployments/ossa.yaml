apiVersion: apps/v1
kind: Deployment
metadata:
  name: ossa
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ossa
  template:
    metadata:
      labels:
        app: ossa
    spec:
      containers:
      - name: ossa
        image: unkaos_ossa:latest
        imagePullPolicy: Never
        ports:
        - containerPort: 3004
        envFrom:
        - configMapRef:
            name: unkaos-config
        - secretRef:
            name: unkaos-secrets
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
        readinessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 5
          periodSeconds: 10
        livenessProbe:
          httpGet:
            path: /health
            port: 3004
          initialDelaySeconds: 15
          periodSeconds: 20
---
apiVersion: v1
kind: Service
metadata:
  name: ossa-service
spec:
  selector:
    app: ossa
  ports:
    - protocol: TCP
      port: 3004
      targetPort: 3004
  type: ClusterIP
