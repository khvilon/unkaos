FROM node:23-alpine

WORKDIR /app/athena

# Копируем сначала только файлы для установки зависимостей
COPY ./server/athena/package.json .
RUN yarn install

# Затем копируем остальные файлы
COPY ./server/athena .

USER node

CMD [ "npx", "ts-node", "index.ts" ]
