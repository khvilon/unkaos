# Автотесты Unkaos (Asclepius)

Автотесты для системы управления задачами Unkaos, написанные на Playwright.

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
cd server/asclepius
npm install
```

### 2. Установка браузеров Playwright

```bash
npm run install:browsers
```

### 3. ✅ Временная почта готова к работе!

**Никакой регистрации и API ключей не требуется!**

Автотесты используют **Guerrilla Mail API** - простой и бесплатный сервис временной почты:
- 🔓 **Без регистрации** - работает сразу
- 🔑 **Без API ключей** - не нужно ничего настраивать  
- 🌐 **Публичный API** - доступен всем
- ⚡ **Быстрый** - мгновенное создание email адресов

### 4. Настройка локального домена

Добавьте в файл hosts:
```
127.0.0.1 unkaos.local
```

**Windows:** `C:\Windows\System32\drivers\etc\hosts`
**Linux/Mac:** `/etc/hosts`

### 5. Запуск системы Unkaos

Убедитесь, что система Unkaos запущена и доступна по адресу `https://unkaos.local:3000`

## 📋 Команды запуска

### Основные команды

```bash
# Запуск всех тестов
npm test

# Запуск с видимым браузером
npm run test:headed

# Запуск с UI интерфейсом Playwright
npm run test:ui

# Запуск в режиме отладки
npm run test:debug

# Запуск только регрессионных тестов
npm run test:regression
```

### Дополнительные опции

```bash
# Запуск конкретного теста
npx playwright test tests/regression.spec.ts

# Запуск с определенным браузером
npx playwright test --project=chromium

# Запуск в headless режиме
npx playwright test --headed=false

# Генерация отчета
npx playwright show-report
```

## 🧪 Структура тестов

```
server/asclepius/
├── tests/
│   └── regression.spec.ts      # Основные регрессионные тесты
├── helpers.ts                  # Вспомогательные функции
├── helpers_workflow.ts         # Функции для работы с воркфлоу
├── playwright.config.ts        # Конфигурация Playwright
├── package.json               # Зависимости и скрипты
├── tsconfig.json              # Конфигурация TypeScript
└── README.md                  # Эта документация
```

## 📝 Текущие тесты

### Регрессионные тесты (`regression.spec.ts`)

1. **Регистрация рабочего пространства и смена пароля**
   - Создание временного email через Guerrilla Mail
   - Регистрация нового workspace
   - Активация через email
   - Смена пароля администратора

2. **Создание пользователя**
   - Добавление нового пользователя в систему
   - Настройка базовых параметров

3. **Создание воркфлоу**
   - Создание нового воркфлоу
   - Настройка статусов и переходов
   - Работа с D3.js редактором

## 🔧 Конфигурация

### Playwright конфигурация (`playwright.config.ts`)

- **Браузер:** Chromium
- **Режим:** Headed (с видимым браузером)
- **Base URL:** `https://unkaos.local:3000`
- **Timeout:** Отключен (0)
- **SSL:** Игнорируются ошибки сертификатов

### 📧 Временная почта - Guerrilla Mail

**API:** `http://api.guerrillamail.com/ajax.php`

**Преимущества:**
- ✅ Без регистрации и API ключей
- ✅ Мгновенное создание email адресов
- ✅ Автоматическое получение писем
- ✅ Парсинг ссылок активации и паролей
- ✅ Поддержка сессий для надежности

**Функции:**
- `getEmailFromTempMail()` - создание временного email
- `getEmails()` - получение списка писем
- `readEmail()` - чтение содержимого письма
- `getIframeBody()` - парсинг активационных данных

## 🐛 Отладка

### Просмотр отчетов

```bash
npx playwright show-report
```

### Трассировка выполнения

```bash
npx playwright test --trace on
```

### Скриншоты при ошибках

Автоматически сохраняются в `test-results/`

### Логи временной почты

В консоли отображаются подробные логи работы с Guerrilla Mail API:
- Создание email адресов
- Получение писем
- Парсинг активационных ссылок

## 📚 Полезные ссылки

- [Playwright Documentation](https://playwright.dev/)
- [Guerrilla Mail](https://www.guerrillamail.com/)
- [Guerrilla Mail API](https://www.guerrillamail.com/GuerrillaMailAPI.html)
- [Unkaos GitHub](https://github.com/khvilon/unkaos)

## 🤝 Разработка

### Добавление новых тестов

1. Создайте новый файл в папке `tests/`
2. Используйте существующие helper функции
3. Следуйте паттернам из `regression.spec.ts`

### Добавление новых helper функций

1. Добавьте функции в `helpers.ts` или создайте новый helper файл
2. Экспортируйте функции для использования в тестах
3. Добавьте JSDoc комментарии для документации

## ⚠️ Известные проблемы

1. **SSL сертификаты:** Игнорируются для локальной разработки
2. **Локальный домен:** Необходимо настроить hosts файл
3. **Guerrilla Mail:** Письма удаляются через 60 минут (достаточно для тестов)

## 🎉 Готово к работе!

Автотесты полностью настроены и готовы к запуску. Временная почта работает без дополнительных настроек!

```bash
cd server/asclepius
npm test
```

## 📞 Поддержка

При возникновении проблем:
1. Проверьте логи в консоли браузера
2. Убедитесь, что система Unkaos запущена
3. Проверьте настройки локального домена
4. Посмотрите отчеты Playwright 